"use strict";  // Enable strict mode (helps catch errors and enforce cleaner code)

// alert("jay"); // Example alert (disabled for now)

// Variable declarations
let name = "jay";         // String
let age = 19;             // Number
let collage;              // Undefined (no value assigned)
let tempreter = null;     // Null (explicit empty value)

/*
 Data Types in JavaScript:
 string     => "text"
 boolean    => true / false
 number     => numeric values
 null       => standalone value (intentional empty)
 undefined  => variable declared but not assigned
 symbol     => unique and immutable value
 object     => collection of key-value pairs
*/

console.log(typeof null);  // Special case: returns "object" (a known JS bug)